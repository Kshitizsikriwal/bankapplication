'use server';

export const SignIn = async () => {
    try {
        // mutation / database / make a fectch 
    } catch (error) {
        console.error('Error', error);
    }
}

export const SignUp = async (userData: SignUpParams) => {
    try {
        // mutation / database / make a fectch 
    } catch (error) {
        console.error('Error', error);
    }
}